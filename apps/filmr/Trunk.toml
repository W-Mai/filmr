[build]
filehash = false
# Exclude depth feature for WASM (ort doesn't support wasm32)
cargo_features = ["cli", "ui", "compute-gpu"]
no_default_features = true

[[hooks]]
stage = "post_build"
command = "python3"
command_arguments = ["patch_dist.py"]

[serve]
headers = { "Cross-Origin-Opener-Policy" = "same-origin", "Cross-Origin-Embedder-Policy" = "require-corp" }
